/// <reference path="../../typings/global.d.ts" />
import { expect } from 'chai';
import { store } from '../../src/store/configStore';

// describe('check store', () => {
//     it('has methods ', () => {
//        expect(store).to.be.a('object');
//        expect(store).to.have.property('dispatch').and.a('function');
//        expect(store).to.have.property('subscribe').and.a('function');
//        expect(store).to.have.property('getState').and.a('function');
//        expect(store).to.have.property('replaceReducer').and.a('function');
//     })
// })
